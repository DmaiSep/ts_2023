import { PipeGradoPipe } from './pipe-grado.pipe';

describe('PipeGradoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeGradoPipe();
    expect(pipe).toBeTruthy();
  });
});
