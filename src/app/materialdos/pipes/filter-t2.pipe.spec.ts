import { FilterT2Pipe } from './filter-t2.pipe';

describe('FilterT2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilterT2Pipe();
    expect(pipe).toBeTruthy();
  });
});
