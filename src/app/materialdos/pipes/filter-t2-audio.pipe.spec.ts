import { FilterT2AudioPipe } from './filter-t2-audio.pipe';

describe('FilterT2AudioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterT2AudioPipe();
    expect(pipe).toBeTruthy();
  });
});
