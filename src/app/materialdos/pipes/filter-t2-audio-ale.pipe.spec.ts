import { FilterT2AudioAlePipe } from './filter-t2-audio-ale.pipe';

describe('FilterT2AudioAlePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterT2AudioAlePipe();
    expect(pipe).toBeTruthy();
  });
});
